# Web框架 & 服务器
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
# 测试与代码分析
pytest>=7.4.0
pytest-html>=3.2.0  # 可选，用于生成测试报告
pylint>=2.17.0      # 用于静态代码分析
# Docker控制
docker>=6.1.0
# HTTP请求 (用于调用LLM API)
requests>=2.31.0
httpx>=0.25.0       # 可选，支持异步HTTP
# OpenAI兼容的SDK (调用多种模型的关键)
openai>=1.3.0
# 环境变量管理 (保护你的API Key)
python-dotenv>=1.0.0